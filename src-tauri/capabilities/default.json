{
  "identifier": "default",
  "description": "Default capability set",
  "windows": ["main", "render-capture"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default"
  ]
}
