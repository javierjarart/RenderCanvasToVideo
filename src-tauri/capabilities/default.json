{
  "identifier": "default",
  "description": "Default capability set",
  "windows": ["main", "render-capture"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default"
  ]
}
